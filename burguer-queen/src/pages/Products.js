import React from "react";
import { Box } from "@mui/material";

const Products = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>Productos</h1>
    </Box>
  );
};

export default Products;
