import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F8A000',
      contrastText: '#FFFFFF',
    },
    secondary: {
      main: '#891E3C',
    },
  },
});

export default theme;